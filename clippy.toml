msrv = "1.96.0"
# lsp-types pins bitflags 1.x and pulldown-cmark uses 2.x; no published lsp-types avoids
# 1.x, so this duplicate is unavoidable under the `lsp` feature. Whitelisting only
# `bitflags` keeps multiple_crate_versions active for every other dependency.
allowed-duplicate-crates = ["bitflags"]
